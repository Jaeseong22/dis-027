# 코어는 표준 라이브러리만 쓴다. 아래 셋은 전부 선택적 의존성이라
# 설치되지 않아도 동작한다(해당 기능만 비활성).
#
#   certifi      기본 CA 번들이 없는 환경에서 LLM HTTPS 호출에 필요
#   pdfplumber   XML 없이 PDF로만 제공된 정기공시 3건의 표 추출
#   pypdf        같은 3건의 텍스트 추출
#
# 설치:  python3 -m pip install -r requirements.txt

certifi>=2024.2.2
pdfplumber>=0.11.0
pypdf>=4.2.0
