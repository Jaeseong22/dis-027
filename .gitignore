# 비밀키 — 절대 커밋 금지
.env
agent2/.env

# 주최 제공 원문(5.2GB). 저장소에 담지 않는다 — `CORPUS_DIR`로 가리킨다.
3.공시/
corpus/

# 실행 산출물·캐시
# ★ `agent2/.cache/`는 **추적한다** — 주최가 전처리 산출물도 제출 대상이라고 안내했다.
#   정본표 2,380 · 정형공시 3,150 · PDF 표 3건 (90MB).
#   재생성: `python3 -m agent2.warmup` + `python3 -m agent2.data.filings`
.cache/
!agent2/.cache/
!agent2/.cache/**
__pycache__/
*.pyc
.DS_Store
