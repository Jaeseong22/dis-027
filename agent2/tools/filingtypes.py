"""공시 유형 레지스트리 — **"○○ 공시가 몇 번 있었나"를 결정론으로 센다.**

## 왜 필요한가

도메인 검수자 1차 질문셋(93문항)의 14문항이 `"…는 몇 번 있었어?"` 형태다. 이건 LLM이 답할
것이 아니라 **공시 목록에서 세면 되는 것**이고, 세면 그 값이 곧 **정답**이다.
평가셋 정답이 아직 없는 상태에서 **우리가 정답을 만들 수 있는 유일한 축**이기도 하다.

세는 대상은 `manifest`(원문 4,204건)가 아니라 **공시 목록 전체 22,980건**이다.
원문이 없어도 "언제 무슨 공시를 냈다"는 확실하기 때문이다.

## 규칙은 추측이 아니라 전수 열거에서 나왔다 (2026-08-12)

2024-01-01 ~ 2026-03-31 구간 17,447건의 `report_nm`을 전수 집계해
**고유 483종**을 눈으로 확인한 뒤 아래 패턴을 정했다. 유형마다 변형이 많다:

    단일판매ㆍ공급계약체결            360   ← 본체
    [기재정정]단일판매ㆍ공급계약체결      398   ← 정정본이 원본보다 많다
    단일판매ㆍ공급계약체결(자율공시)       22   ← 같은 사건, 공시 근거만 다르다
    단일판매ㆍ공급계약해지              17   ← **다른 사건**이다. 제외한다

## ★ `is_correction` 필드를 쓰지 않는다 — 목록에서는 비어 있다

    원문이 있는 4,204건        제목의 `[기재정정]` ↔ `is_correction`  **4,204/4,204 일치**
    목록에만 있는 18,776건     `[기재정정]` 1,053건인데 `is_correction`은 **0건**

즉 주최가 준 목록 데이터의 이 필드는 **원문 수집분에만 채워져 있다.** 목록 전체를
세는 이 모듈은 **제목 접두**(`[기재정정]`·`[첨부정정]`·`[첨부추가]`)로 판별한다.
(원문 기반인 `corrections.py`·`docsections.py`는 필드를 써도 안전하다 — 거기는 100% 일치한다.)

## 무엇을 "한 번"으로 세는가 — 원본 건수가 기본값이다

`"몇 번 있었어?"`는 **사건 횟수**를 묻는다. 정정본은 같은 사건의 재공시다.
삼성E&A 단일판매 71건 중 원본은 24건인데, 정정을 함께 세면 3배로 부풀어
"71번 계약했다"는 틀린 답이 된다.

그래서 `count()`는 세 수를 함께 돌려준다 — 어느 것이 답인지는 질문이 정한다.

    원본      정정 표기가 없는 건        ← 기본값(사건 수)
    정정      정정 표기가 있는 건
    전체      원본 + 정정 (문서 수)

목록만 있는 건은 원공시 참조 정보(`정정관련 공시서류제출일`)가 원문에 있어서
체인을 만들 수 없다. 그래서 체인 병합이 아니라 **원본 계수**로 센다. 이 한계를
`note`에 명시한다 — 정정본이 원본 없이 목록에 남은 경우 과소계수가 된다.

## 원문이 없는 유형은 "누락"이 아니라 **수집 대상이 아니다**

코퍼스 `data_filter.md`가 수집 문서를 명시한다 — 거래소공시는 **4종만**이다:
`단일판매ㆍ공급계약체결/해지` · `신규시설투자등` · `투자판단관련주요경영사항`.
그래서 `감사보고서제출`·`현금ㆍ현물배당결정`·`기업설명회` 등은 목록(list_*.json)에만
있고 원문이 0건이다. 배포가 빠진 것이 아니라 처음부터 안 받은 것이다.

    그룹        목록      원문     보유율
    periodic   1,166   1,054    90.4%
    major        639     598    93.6%
    exchange  10,921   1,469    13.5%   ← 4종만 수집
    holding   10,254   1,083    10.6%   ← 대량보유상황보고서만 수집

따라서 이 모듈은 **건수·날짜**까지만 답한다. 본문 값(배당금·취득금액 등)은
원문이 있는 유형에서만 나온다. 없는 것은 정보한계로 답하는 것이 정답이다.

## 자회사/종속회사 공시는 분리한다

    감사보고서제출                     207
    감사보고서제출(자회사의 주요경영사항)      45   ← 보고 주체가 자회사다

`"이 회사의 감사보고서제출은 몇 번"`에 자회사 건을 섞으면 안 된다. 기본은 본체만
세고 자회사 건수를 따로 밝힌다.

실행:
    python3 -m agent2.tools.filingtypes            # 전 유형 × 전 기업 요약
    python3 -m agent2.tools.filingtypes 삼성전자     # 한 기업
"""
import re
import sys
from collections import namedtuple

from agent2.data import store

#: **코퍼스 전 구간.** 주최가 준 데이터는 전부 쓴다(사용자 결정 2026-08-23).
#:
#: ★ 종전 값은 `("20240101", "20260331")`이었고 근거가 "질문셋이 지정한 기간 —
#:   도메인 검수자 1차 작업물이 전부 '2024년~2026년 3월까지'로 시작한다"였다.
#:   **그 질문셋은 우리 산출물이지 코퍼스가 아니다.** 실측하면:
#:       공시목록 접수일 실제 범위   20230102 ~ 20260619  (22,980건)
#:       종전 PERIOD 가 덮던 범위    17,447건 = **75.9%**
#:       잘려 나가던 것              앞 5,454건(2023년 전체) + 뒤 79건(2026년 1분기 보고서)
#:   명세도 `data_filter.md [3]`이 주요사항·거래소·지분을 `2023.01.01 ~ 2026.03.31`로
#:   정의한다. 정기공시 2026년 1분기분은 5~6월에 접수되어 20260619까지 간다.
#:
#: ★★ **기간을 좁히려면 호출부가 `period`를 명시하라.** 질문이 기간을 말하면
#:   `list_filings(start=…, end=…)`로 넘어온다(실측: 그런 질문에서 81.6%가 넘긴다).
#:   정답 생성기는 자기 `PERIOD`를 따로 갖고 명시적으로 넘긴다 — 그 정답 문항
#:   112건이 전부 질문에 "2024년~2026년 3월"을 못 박고 있기 때문이다.
PERIOD = ("20230101", "20261231")

#: ★★ **질문 창** — 평가 질의가 말하는 기간("2024년~2026년 3월")이다. `PERIOD`와 다르다.
#:
#:   `PERIOD`는 **코퍼스가 가진 전 구간**이고(§2.9 ③에서 넓혔다), 이쪽은 **질문이
#:   지정한 구간**이다. 둘을 섞으면 안 된다 — 전자는 데이터의 사실이고 후자는 우리 산출물이다.
#:
#:   왜 상수로 두나: 이 값이 `tools/__init__.list_filings`와 `tools/__init__.correction_history`
#:   **두 곳에 문자열로 하드코딩**돼 있었다(2026-08-26 발견). 한쪽만 고치면 같은 질문이
#:   도구에 따라 다른 창을 세게 된다 — 이 저장소가 반복해 겪은 "규칙을 두 곳에 두면
#:   어긋난다"(§6-2)이다. 값을 바꾸지 않고 **이름만 하나로 모은다.**
#:
#:   ★ `PERIOD`로 바꾸지 않는 이유: 정답셋 115문항의 계수 정답이 이 창 기준이다
#:     (`공시목록 20240101~20260331`). 넓히면 2023년 공시가 더 세어져 정답과 어긋난다.
#:     질문이 다른 기간을 말하면 `loop._with_period`가 `start`/`end`를 주입한다.
QUESTION_PERIOD = ("20240101", "20260331")

#: 제목 접두 = 정정 표기. **이 접두가 판별 기준이다**(위 docstring 참조).
#: 전수: `[기재정정]` 2,057 · `[첨부정정]` 134 · `[첨부추가]` 34 · `[연장결정]` 1.
#:
#: ★ **`[첨부추가]`는 정정이 아니다**(2026-08-12 공식 명세 대조로 정정).
#:   코퍼스 README: *"`[기재정정]` 외 태그 공시는 수집 대상이 아니나, 원본이
#:   `[첨부추가]`본으로만 제공되는 공시 **6건**은 해당 본을 **원본으로** 수집했습니다
#:   (`is_correction=false`)."*
#:   실측도 같다 — manifest의 `[첨부추가]` 6건 전부 `is_correction=False`
#:   (고려아연·우리금융지주×2·두산에너빌리티·한화솔루션·HD현대일렉트릭).
#:   이걸 정정으로 세면 **원본 건수가 과소계수**된다.
#: `[연장결정]`도 정정이 아니라 별도 사건이므로 넣지 않는다.
_CORR_PREFIX = ("[기재정정]", "[첨부정정]")

#: 보고 주체가 자회사·종속회사임을 나타내는 꼬리표. 본체 계수에서 뺀다.
_SUB_MARK = ("자회사의 주요경영사항", "종속회사의주요경영사항", "종속회사의 주요경영사항")

_PRE = re.compile(r"^\s*\[[^\]]+\]\s*")
_SP = re.compile(r"[\s　]+")


def _norm(name):
    """보고서명 정규화 — 정정 접두를 떼고 공백을 지운다.

    공백을 지우는 이유: 같은 유형이 `대표이사(대표집행임원)변경`과
    `대표이사 (대표집행임원) 변경`처럼 띄어쓰기만 달리 오는 경우가 있다.
    """
    return _SP.sub("", _PRE.sub("", (name or "").strip()))


def is_correction(row):
    """정정본인가 — **제목 접두로만** 판별한다(`is_correction` 필드는 목록에서 빈다)."""
    return (row.get("report_nm") or "").strip().startswith(_CORR_PREFIX)


def is_subsidiary(row):
    """보고 주체가 자회사·종속회사인가."""
    return any(m in (row.get("report_nm") or "") for m in _SUB_MARK)


#: 공시 유형 1건.
#:   id       질문셋에서 부르는 이름
#:   label    사람이 읽는 이름
#:   include  정규화된 보고서명이 이 중 하나로 **시작**하면 포함
#:   exclude  포함됐더라도 이 조각이 들어 있으면 제외
#:   group    manifest의 doc_group(검증용. 목록에는 없을 수 있어 필수는 아니다)
FilingType = namedtuple("FilingType", "id label include exclude group")


def F(fid, label, include, exclude=(), group=None):
    return FilingType(fid, label, tuple(_SP.sub("", x) for x in include),
                      tuple(_SP.sub("", x) for x in exclude), group)


#: ★ 각 항목 뒤 숫자는 **2024-01-01~2026-03-31 전수 실측**(원본+정정, 자회사 포함)이다.
#:   패턴을 고칠 때 이 숫자가 크게 흔들리면 규칙이 바뀐 것이므로 다시 확인할 것.
#:
#: ★ **2026-08-15 전수 재측정으로 5종을 정정했다.** 낡은 값이 남아 있으면 감시선이
#:   아니라 잡음이 된다(자기주식처분 116→117 · 유상증자 130→105 · 현금배당 264→318 ·
#:   타법인취득 140→172 · 손익구조 234→237). 자회사분이 큰 유형은 괄호로 함께 적는다 —
#:   `count()`가 자회사를 빼고 세므로 숫자를 대조할 때 어느 기준인지 알아야 한다.
#:   같은 감사에서 **분류 자체는 17종 전부 정확**했다(다중 매칭 0건, 제외 규칙
#:   `결과`·`처분`·`주주명부폐쇄`·`미만`·`의견표명` 모두 의도대로 동작).
TYPES = (
    # ── 주요사항보고서 ──────────────────────────────────────────
    F("자기주식취득결정", "자기주식 취득 결정",
      ("주요사항보고서(자기주식취득결정)",),
      # 신탁계약 체결·해지는 **다른 결정**이다(취득 자체가 아니라 위탁 계약).
      ("신탁계약",), "major"),                                    # 71
    F("자기주식처분결정", "자기주식 처분 결정",
      ("주요사항보고서(자기주식처분결정)",), (), "major"),            # 117
    F("유상증자결정", "유상증자 결정",
      ("주요사항보고서(유상증자결정)", "유상증자결정"),
      # 청약·발행 '결과'는 결정이 아니다.
      ("결과",), "major"),                                        # 105 (자회사 68)
    F("무상증자결정", "무상증자 결정",
      ("주요사항보고서(무상증자결정)", "무상증자결정"), ("결과",), "major"),     # 4

    # ── 주식관련사채 3종 (CB·BW·EB) ─ 2026-08-17 추가 ────────────────
    #
    # ★ 왜: 주최 참고용 질의가 "자금조달 내역을 **유형별(유상증자, CB, BW, EB)**로
    #   정리해줘"인데 증자 2종만 있어 **유형 자체를 못 받았다.** 원문은 있었다.
    # ★ 유형 정의를 지어내지 않았다 — **공시목록 22,980건 전수에서 서식명을 읽어**
    #   정했다(§2). 실측 서식명과 건수:
    #       주요사항보고서(전환사채권발행결정)          23건 ·  8사   ← CB
    #       주요사항보고서(교환사채권발행결정)           4건 ·  2사   ← EB
    #       신주인수권부사채 **발행결정** 서식            **0건**      ← BW
    #   BW로 잡히는 것은 `…발행후만기전사채취득`(소각) ·
    #   `전환청구권ㆍ신주인수권ㆍ교환청구권행사`(행사) ·
    #   `전환가액ㆍ신주인수권행사가액ㆍ교환가액의조정`(조정)뿐이고 **전부 발행이 아니다.**
    #   → BW는 **등록하되 계수가 0으로 나오게** 둔다. 그래야 "이 기간에 BW 발행은
    #     없습니다"라고 근거를 갖고 답한다(§1-3 정직한 None). 유형을 빼면 모델이
    #     "확인할 수 없다"고만 하거나 다른 사채를 BW로 답할 위험이 있다.
    # ★ 제외 규칙도 실측에서 나왔다 — 같은 `전환사채` 어휘를 쓰지만 발행이 아닌 것들:
    #       주요사항보고서(자기전환사채매도결정)          2건   매도
    #       주요사항보고서(제3자의전환사채매수선택권행사)   1건   행사
    #       전환사채(해외전환사채포함)발행후만기전사채취득   7건   취득(소각)
    #   ★ `_match`는 **prefix 매칭**(`startswith`)이라 `주요사항보고서(…)` 전체 형태를
    #     함께 넣는다 — `유상증자결정`이 두 형태를 넣은 이유와 같다. 짧은 것만 넣었더니
    #     전수 23건이 **0건으로 세어졌다**(§6-20: 검사기를 먼저 의심하라).
    F("전환사채발행결정", "전환사채(CB) 발행 결정",
      ("주요사항보고서(전환사채권발행결정)", "전환사채권발행결정"),
      ("매도", "매수선택권", "취득", "결과"), "major"),                # 23 (8사)
    F("신주인수권부사채발행결정", "신주인수권부사채(BW) 발행 결정",
      ("주요사항보고서(신주인수권부사채권발행결정)", "신주인수권부사채권발행결정",
       "주요사항보고서(신주인수권부사채발행결정)", "신주인수권부사채발행결정"),
      ("취득", "행사", "조정", "결과"), "major"),                     # **0** — 코퍼스에 없다
    # ── 계약 해지 2종 ─ 2026-08-17 추가 ───────────────────────────
    #
    # ★ 왜: 주최 참고용 질의가 "체결한 주요 계약 **이후 해지된 계약이 존재하는가**"인데
    #   해지 유형이 없어 계수·분류가 안 됐다. 원문은 있었다(전수 실측):
    #       단일판매ㆍ공급계약해지                  20건
    #       주요사항보고서(자기주식취득신탁계약해지결정)  43건
    # ★ **해지 공시가 원계약을 스스로 가리킨다** — `8. 기타 투자판단과 관련한 중요사항 >
    #   관련공시` 필드가 `2024-10-15 단일판매ㆍ공급계약체결` 형식이고 **19/20건이 보유**한다.
    #   그 날짜로 원계약을 찾으면 **8/19건**이 코퍼스에 있고, 나머지 11건은 원계약이
    #   2015~2022년이라 **코퍼스 기간(2023~) 밖**이다 — 결함이 아니라 데이터 한계이고
    #   "정보한계 대응"(주최 평가지표)으로 답해야 할 자리다.
    # ★ 설비투자 — 사업보고서 `capex` 정본표가 **36/70사**뿐이라 나머지를 못 답했다.
    #   전수: `신규시설투자등` 43건 · 21사이고 필드가 완전하다
    #   (`2. 투자내역 > 투자금액(원)` 43/43 · 투자구분·투자목적·투자기간 43/43).
    #   그중 **capex 표가 없는 회사 5사**를 이 공시가 덮는다(에코프로비엠 등).
    F("신규시설투자등", "신규 시설투자 등",
      ("신규시설투자등",), ("결과",), "exchange"),                     # 43 (21사)

    F("단일판매공급계약해지", "단일판매ㆍ공급계약 해지",
      ("단일판매ㆍ공급계약해지", "단일판매공급계약해지"), (), "exchange"),      # 20
    # ★ **명세 [2]가 명시한 거래소 4종 중 마지막.** 이 모듈 docstring이 4종으로 적어
    #   놓고도 TYPES 에는 없어서 `count()`가 KeyError 였다(2026-08-23 전수 감사에서 발견).
    #   전수: 목록 300건 = 원문 300건(100%) · 36사 · 원본 247 · 정정 53.
    #   계수가 없으면 모델이 목록을 세는데 `limit`(기본 10)에 잘린다 —
    #   **셀트리온이 99건**이다(삼성바이오로직스 34 · 대우건설 27 · 현대건설 21).
    #   제목 뒤에 사유가 괄호로 붙는 변형이 많아(`투자판단관련주요경영사항(임상시험결과)`)
    #   `startswith` 로 잡는다. 자회사 건은 `is_subsidiary` 가 따로 가른다.
    F("투자판단관련주요경영사항", "투자판단 관련 주요경영사항",
      ("투자판단관련주요경영사항",), (), "exchange"),                    # 300 (36사)

    # ── 주요사항보고서 나머지 21종 (2026-08-23 전수 감사) ──────────────
    #   원문이 있는데 유형이 없어 `count()`가 KeyError 였다 — 목록 256건 · 원문 220건.
    #   ★ `해외증권시장주권등상장` / `…상장결정` / `…상장폐지` / `…상장폐지결정` 네 개는
    #     서로 **접두가 겹친다**. include 에 **닫는 괄호까지** 넣어야 갈린다.
    #     (B-7 정답이 `주요사항보고서(해외증권시장주권등상장폐지)` 라 특히 중요하다)
    F("상각형조건부자본증권발행결정", "상각형 조건부자본증권 발행 결정",
      ("주요사항보고서(상각형조건부자본증권발행결정)",), (), "major"),                 # 72 (원문 71 · 4사)
    F("자기주식취득신탁계약체결결정", "자기주식 취득 신탁계약 체결 결정",
      ("주요사항보고서(자기주식취득신탁계약체결결정)",), (), "major"),                 # 49 (원문 47 · 13사)
    F("회사합병결정", "회사 합병 결정",
      ("주요사항보고서(회사합병결정)",), (), "major"),                         # 41 (원문 24 · 14사)
    F("회사분할결정", "회사 분할 결정",
      ("주요사항보고서(회사분할결정)",), (), "major"),                         # 18 (원문 12 · 6사)
    F("주식교환ㆍ이전결정", "주식 교환ㆍ이전 결정",
      ("주요사항보고서(주식교환ㆍ이전결정)",), (), "major"),                      # 17 (원문 12 · 5사)
    F("회사분할합병결정", "회사 분할합병 결정",
      ("주요사항보고서(회사분할합병결정)",), (), "major"),                       # 9 (원문 8 · 1사)
    F("타법인주식및출자증권양수결정", "타법인 주식·출자증권 양수 결정",
      ("주요사항보고서(타법인주식및출자증권양수결정)",), (), "major"),                 # 8 (원문 7 · 3사)
    F("감자결정", "감자 결정",
      ("주요사항보고서(감자결정)",), (), "major"),                           # 6 (원문 6 · 3사)
    F("자본으로인정되는채무증권발행결정", "자본인정 채무증권 발행 결정",
      ("주요사항보고서(자본으로인정되는채무증권발행결정)",), (), "major"),               # 6 (원문 6 · 3사)
    F("유형자산양도결정", "유형자산 양도 결정",
      ("주요사항보고서(유형자산양도결정)",), (), "major"),                       # 5 (원문 3 · 2사)
    F("소송등의제기", "소송 등의 제기",
      ("주요사항보고서(소송등의제기)",), (), "major"),                         # 5 (원문 5 · 1사)
    F("해외증권시장주권등상장폐지", "해외 증권시장 주권등 상장폐지",
      ("주요사항보고서(해외증권시장주권등상장폐지)",), (), "major"),                  # 4 (원문 4 · 4사)
    F("해외증권시장주권등상장폐지결정", "해외 증권시장 주권등 상장폐지 결정",
      ("주요사항보고서(해외증권시장주권등상장폐지결정)",), (), "major"),                # 4 (원문 4 · 4사)
    F("유형자산양수결정", "유형자산 양수 결정",
      ("주요사항보고서(유형자산양수결정)",), (), "major"),                       # 3 (원문 2 · 1사)
    F("영업양수결정", "영업 양수 결정",
      ("주요사항보고서(영업양수결정)",), (), "major"),                         # 2 (원문 2 · 1사)
    F("자기전환사채매도결정", "자기 전환사채 매도 결정",
      ("주요사항보고서(자기전환사채매도결정)",), (), "major"),                     # 2 (원문 2 · 1사)
    F("타법인주식및출자증권양도결정", "타법인 주식·출자증권 양도 결정",
      ("주요사항보고서(타법인주식및출자증권양도결정)",), (), "major"),                 # 1 (원문 1 · 1사)
    F("영업정지", "영업 정지",
      ("주요사항보고서(영업정지)",), (), "major"),                           # 1 (원문 1 · 1사)
    F("해외증권시장주권등상장", "해외 증권시장 주권등 상장",
      ("주요사항보고서(해외증권시장주권등상장)",), (), "major"),                    # 1 (원문 1 · 1사)
    F("해외증권시장주권등상장결정", "해외 증권시장 주권등 상장 결정",
      ("주요사항보고서(해외증권시장주권등상장결정)",), (), "major"),                  # 1 (원문 1 · 1사)
    F("제3자의전환사채매수선택권행사", "제3자의 전환사채 매수선택권 행사",
      ("주요사항보고서(제3자의전환사채매수선택권행사)",), (), "major"),                # 1 (원문 1 · 1사)
    F("자기주식신탁해지", "자기주식 취득 신탁계약 해지 결정",
      ("주요사항보고서(자기주식취득신탁계약해지결정)",), (), "major"),         # 43

    F("교환사채발행결정", "교환사채(EB) 발행 결정",
      ("주요사항보고서(교환사채권발행결정)", "교환사채권발행결정"),
      ("취득", "행사", "조정", "결과"), "major"),                     # 4 (2사)

    # ── 거래소공시 ──────────────────────────────────────────────
    F("감사보고서제출", "감사보고서 제출",
      ("감사보고서제출",), (), "exchange"),                          # 279
    F("정기주주총회결과", "정기주주총회 결과",
      ("정기주주총회결과",), (), "exchange"),                        # 212
    F("현금배당결정", "현금ㆍ현물배당 결정",
      ("현금ㆍ현물배당결정",),
      # 주주명부폐쇄(기준일) 결정은 배당 결정 자체가 아니다.
      ("주주명부폐쇄", "기준일",), "exchange"),                       # 318 (자회사 54)
    F("타법인취득결정", "타법인주식및출자증권 취득 결정",
      ("타법인주식및출자증권취득결정",),
      ("처분",), "exchange"),                                       # 172 (자회사 52)
    F("지속가능경영보고서", "지속가능경영보고서 등 관련사항",
      ("지속가능경영보고서등관련사항",), (), "exchange"),               # 131
    F("대표이사변경", "대표이사(대표집행임원) 변경 안내공시",
      ("대표이사(대표집행임원)변경",), (), "exchange"),                # 66
    F("단일판매공급계약체결", "단일판매ㆍ공급계약 체결",
      ("단일판매ㆍ공급계약체결",),
      # 해지는 **반대 사건**이다. 체결 건수에 섞으면 안 된다.
      ("해지",), "exchange"),                                       # 804
    F("최대주주소유주식변동", "최대주주등 소유주식변동신고서",
      ("최대주주등소유주식변동신고서",), (), "exchange"),               # 575
    F("손익구조변경", "매출액 또는 손익구조 30%(대규모법인 15%) 이상 변경",
      # `…이상변경`과 `…이상변동` 두 표기가 함께 쓰인다(158 vs 25). 둘 다 같은 서식이다.
      ("매출액또는손익구조30%(대규모법인은15%)이상",),
      # `미만변경(자율공시)`은 **반대 조건**이다.
      ("미만",), "exchange"),                                       # 237 (자회사 37)
    F("기업설명회", "기업설명회(IR) 개최 안내공시",
      ("기업설명회(IR)개최",), (), "exchange"),                      # 1,364

    # ── 지분공시 ────────────────────────────────────────────────
    F("대량보유상황보고", "주식등의 대량보유상황보고서",
      ("주식등의대량보유상황보고서",), (), "holding"),                  # 767
    F("임원주요주주소유상황", "임원ㆍ주요주주특정증권등 소유상황보고서",
      ("임원ㆍ주요주주특정증권등소유상황보고서",),
      # '거래계획보고서'는 다른 서식이다(사전 공시).
      ("거래계획",), "holding"),                                     # 6,950
    F("의결권대리행사권유", "의결권 대리행사 권유 참고서류",
      ("의결권대리행사권유참고서류",), ("의견표명",), "holding"),        # 365
)

BY_ID = {t.id: t for t in TYPES}


def _match(row, ft):
    n = _norm(row.get("report_nm"))
    if not any(n.startswith(p) for p in ft.include):
        return False
    return not any(x in n for x in ft.exclude)


def rows_of(corp, type_id, period=PERIOD, include_sub=False):
    """그 회사·그 유형의 공시 행 목록(기간 필터 적용)."""
    ft = BY_ID.get(type_id)
    if ft is None:
        raise KeyError(f"미등록 공시 유형: {type_id} — TYPES에 먼저 등록할 것")
    c = store.resolve_corp(corp)
    if c is None:
        return []
    lo, hi = period or ("", "99999999")
    out = []
    for r in store.docs(corp=c["corp_name"], source_only=False):
        dt = str(r.get("rcept_dt") or "")
        if not (lo <= dt <= hi):
            continue
        if not _match(r, ft):
            continue
        if not include_sub and is_subsidiary(r):
            continue
        out.append(r)
    out.sort(key=lambda r: (r.get("rcept_dt") or "", r.get("rcept_no") or ""))
    return out


def count(corp, type_id, period=PERIOD):
    """건수 — **원본/정정/전체를 함께** 준다. 어느 것이 답인지는 질문이 정한다.

    `"몇 번 있었어?"`의 답은 `원본`이다(사건 수). `전체`는 문서 수다.
    """
    body = rows_of(corp, type_id, period, include_sub=False)
    sub = rows_of(corp, type_id, period, include_sub=True)
    sub = [r for r in sub if is_subsidiary(r)]
    orig = [r for r in body if not is_correction(r)]
    corr = [r for r in body if is_correction(r)]
    have = {r["rcept_no"] for r in store.manifest()}
    return {
        "corp": corp, "유형": BY_ID[type_id].label, "기간": f"{period[0]}~{period[1]}",
        "원본": len(orig), "정정": len(corr), "전체": len(body),
        "자회사건": len(sub),
        "원문보유": sum(1 for r in body if r["rcept_no"] in have),
        "최근": (body[-1].get("rcept_dt") if body else None),
        "접수번호": [r["rcept_no"] for r in orig[-3:]],
        # ★ 기간을 말로 다시 못 박는다. 기본값이 **코퍼스 전 구간**이 되면서
        #   "2024년~2026년 3월" 같은 질문에서 모델이 start/end 를 안 넘기면
        #   전 구간을 세게 된다(실측: 그런 질문에서도 18.4%는 안 넘긴다).
        #   `기간` 필드만으로는 모델이 안 읽는다 — note 에 문장으로 적는다.
        "note": (f"이 계수는 **{period[0]}~{period[1]}** 기간입니다. "
                 "질문이 다른 기간을 말하면 start·end 를 주고 다시 부르십시오. "
                 "'몇 번'의 답은 **원본** 건수입니다(정정본은 같은 사건의 재공시). "
                 "정정본만 목록에 남은 경우 과소계수가 될 수 있습니다."),
    }


def latest(corp, type_id, period=PERIOD, with_source=True):
    """가장 최근 공시 1건. `with_source`면 **원문이 있는 것** 중 최신을 고른다.

    `"가장 최근 공시의 …계약금액은?"` 유형은 원문을 읽어야 답할 수 있으므로,
    원문 없는 최신본을 집어 주면 답을 못 만든다. 정정본이 있으면 **정정본이 유효본**이다.
    """
    rows = rows_of(corp, type_id, period)
    if not rows:
        return None
    if with_source:
        have = {r["rcept_no"] for r in store.manifest()}
        src = [r for r in rows if r["rcept_no"] in have]
        if src:
            return src[-1]
    return rows[-1]


def _smoke(corp=None):
    corps = [corp] if corp else ["삼성전자", "SK하이닉스", "한화오션", "KB금융"]
    for c in corps:
        print(f"\n=== {c}")
        for t in TYPES:
            r = count(c, t.id)
            if r["전체"] or r["자회사건"]:
                print(f"   {t.label[:30]:32s} 원본{r['원본']:4d} 정정{r['정정']:4d} "
                      f"자회사{r['자회사건']:3d} 원문{r['원문보유']:4d}")


if __name__ == "__main__":
    _smoke(sys.argv[1] if len(sys.argv) > 1 else None)
