"""2호기 — PAO(계획·행동·관찰) 루프 에이전트.

1호기(`agent/`)와의 관계:
  · 1호기 = 단일 턴 결정론 파이프라인. 코드가 제어하고 LLM은 문장만 쓴다.
  · 2호기 = LLM이 루프를 돌며 툴을 고른다. 액션은 1호기의 결정론 추출기를 그대로 쓴다.
  → 2호기는 1호기의 재작성이 아니라 **상위집합**이다. `agent.*`는 import만 하고 고치지 않는다.

비교가 성립하려면 세 가지가 같아야 한다(설계 규칙 D-3):
  ① 동일 계약(5필드)  ② 동일 채점기(agent.eval)  ③ 동일 코퍼스
이 셋 중 하나라도 다르면 A/B가 아니라 서로 다른 두 실험이 된다.

로컬 전용: `.git/info/exclude`에 등재 — 깃허브에 올라가지 않는다.
"""
